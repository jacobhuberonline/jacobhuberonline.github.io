---
import '../styles/global.css';
import { withBase } from '../lib/paths';
import SocialLinks from '../components/SocialLinks.astro';
import Icon from '../components/Icon.astro';
import CloudflareAnalytics from '../components/CloudflareAnalytics.astro';
import { pageSchema } from '../lib/seo';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  imageAlt?: string;
  imageWidth?: number;
  imageHeight?: number;
  imageType?: string;
  hideChrome?: boolean;
  noindex?: boolean;
}

const {
  title,
  description = 'Business websites, redesigns, and workflow automation by Jacob Huber. Based in Edwardsville, Illinois, working with local and nationwide clients.',
  image = '/images/huber-builds-social.png',
  imageAlt = 'Huber Builds: Business websites. Built around you. Website design, ongoing care, and workflow automation at huberbuilds.com.',
  imageWidth = 1200,
  imageHeight = 630,
  imageType = 'image/png',
  hideChrome = false,
  noindex = false,
} = Astro.props;
const pageTitle = title ? `${title} · Huber Builds` : 'Business Websites & Workflow Automation · Huber Builds';
const canonical = new URL(Astro.url.pathname, Astro.site);
const socialImage = new URL(withBase(image), Astro.site);
const structuredData = Astro.site && !noindex ? pageSchema(Astro.site, canonical, pageTitle, description) : undefined;
const navItems = [
  { href: '/services/', label: 'Services' },
  { href: '/projects/', label: 'My work' },
  { href: '/about/', label: 'About me' },
  { href: '/contact/', label: 'Start a project' },
];
const isActive = (href: string) => Astro.url.pathname === withBase(href)
  || (href !== '/' && Astro.url.pathname.startsWith(withBase(href)));
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="theme-color" content="#fff9ef" />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <meta name="author" content="Jacob Huber" />
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/svg+xml" href={withBase('/favicon.svg')} />
    {noindex && <meta name="robots" content="noindex, follow" />}
    <meta property="og:type" content="website" />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:site_name" content="Huber Builds" />
    <meta property="og:image" content={socialImage} />
    <meta property="og:image:alt" content={imageAlt} />
    <meta property="og:image:width" content={String(imageWidth)} />
    <meta property="og:image:height" content={String(imageHeight)} />
    <meta property="og:image:type" content={imageType} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={socialImage} />
    <meta name="twitter:image:alt" content={imageAlt} />
    {structuredData && <script is:inline type="application/ld+json" set:html={JSON.stringify(structuredData).replace(/</g, '\\u003c')} />}
  </head>
  <body>
    <a href="#main-content" class="skip-link">Skip to content</a>
    {!hideChrome && (
      <header class="site-header">
        <div class="container header-inner">
          <a href={withBase('/')} aria-label="Huber Builds home" class="wordmark">
            <span class="monogram" aria-hidden="true">jh.</span>
            <span>Huber Builds<span class="wordmark-note">Websites & automation</span></span>
          </a>
          <nav aria-label="Main navigation" class="desktop-navigation">
            {navItems.map((item) => (
              <a href={withBase(item.href)} class:list={[{ 'nav-cta': item.href === '/contact/', 'is-active': isActive(item.href) }]} aria-current={Astro.url.pathname === withBase(item.href) ? 'page' : undefined}>{item.label}</a>
            ))}
          </nav>
          <details class="mobile-menu">
            <summary>Menu <span aria-hidden="true">+</span></summary>
            <nav aria-label="Mobile navigation">
              <a href={withBase('/')} aria-current={isActive('/') ? 'page' : undefined}>Home</a>
              {navItems.map((item) => (
                <a href={withBase(item.href)} class:list={[{ 'nav-cta': item.href === '/contact/', 'is-active': isActive(item.href) }]} aria-current={Astro.url.pathname === withBase(item.href) ? 'page' : undefined}>{item.label}</a>
              ))}
            </nav>
          </details>
        </div>
      </header>
    )}
    <main id="main-content" tabindex="-1"><slot /></main>
    {!hideChrome && (
      <footer class="site-footer">
        <div class="container">
          {!isActive('/contact/') && <div class="footer-top">
            <h2>Let’s talk about your project.</h2>
            <a href={withBase('/contact/')} class="button button-light">Get a project estimate <Icon name="arrow-up-right" size={18} /></a>
          </div>}
          <div class="footer-bottom">
            <p>© <span id="copyright-year">2026</span> Huber Builds by Jacob Huber <span class="footer-location">· Edwardsville, Illinois</span></p>
            <div class="footer-links">
              <nav aria-label="Footer navigation">
                <a href={withBase('/')}>Home</a>
                <a href={withBase('/experience/')}>Résumé</a>
                <a href="mailto:jhuber.mail@icloud.com">Email</a>
              </nav>
              <SocialLinks />
            </div>
          </div>
        </div>
      </footer>
    )}
    <script>
      const copyrightYear = document.getElementById('copyright-year');
      if (copyrightYear) copyrightYear.textContent = String(new Date().getFullYear());
      const menu = document.querySelector<HTMLDetailsElement>('.mobile-menu');
      const header = document.querySelector<HTMLElement>('.site-header');
      if (header) {
        // Clamp overscroll so the bounce at either end doesn't reverse the header.
        const scrollPosition = () => Math.max(0, Math.min(
          window.scrollY,
          document.documentElement.scrollHeight - window.innerHeight,
        ));
        let lastPosition = scrollPosition();
        let direction = 0;
        let distance = 0;

        const showHeader = () => {
          header.classList.remove('is-hidden');
          lastPosition = scrollPosition();
          direction = 0;
          distance = 0;
        };

        window.addEventListener('scroll', () => {
          const position = scrollPosition();
          const delta = position - lastPosition;
          lastPosition = position;

          if (position <= header.offsetHeight || menu?.open || header.querySelector(':focus-visible')) {
            showHeader();
            return;
          }
          if (delta === 0) return;

          const nextDirection = Math.sign(delta);
          distance = nextDirection === direction ? distance + Math.abs(delta) : Math.abs(delta);
          direction = nextDirection;

          // Ignore tiny scroll reversals from a trackpad or touch gesture.
          if (distance >= 10) {
            header.classList.toggle('is-hidden', direction > 0);
            distance = 0;
          }
        }, { passive: true });

        header.addEventListener('focusin', showHeader);
        menu?.addEventListener('toggle', () => {
          if (menu.open) showHeader();
        });
        window.addEventListener('resize', () => {
          if (menu && menu.offsetParent === null) menu.open = false;
          showHeader();
        });
        window.addEventListener('pageshow', showHeader);
      }
      document.addEventListener('keydown', (event) => {
        if (event.key === 'Escape' && menu?.open) {
          menu.open = false;
          menu.querySelector('summary')?.focus();
        }
      });
    </script>
    <CloudflareAnalytics />
  </body>
</html>
